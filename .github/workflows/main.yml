name: main

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened
  workflow_call: {}

env:
  CI: true

jobs:
  main:
    timeout-minutes: 15
    name: 'main'
    runs-on: ubuntu-latest
    steps:
      - name: 'hello world'
        run: 'echo hello world'
