name: main

on:
  workflow_run:
    workflows: [Release]
    types: [requested]
    branches: [changeset-release/main]

env:
  CI: true

jobs:
  main:
    timeout-minutes: 15
    name: 'main'
    runs-on: ubuntu-latest
    steps:
      - name: 'hello world'
        run: 'echo hello world'
